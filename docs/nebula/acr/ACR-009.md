# ACR-009 — ACI-009 acceptance

**ACI:** ACI-009 — ADE Live AI Performance Analytics & Recommendations  
**Date recorded:** 2026-08-26  
**Status:** COMPLETE  
**Product changes:** live AI analysis/recommendations on persisted Goal, content, and manual metrics; deterministic analytics preserved

## Accepted outcome

An operator can request live AI analysis from Intelligence without leaving ADE. ADE sends persisted Goal / Campaign / content / results evidence to the existing OpenAI provider boundary, stores an advisory recommendation with Observed / Meaning / Recommended next action, and keeps the linked evidence list. Default `POST /api/intelligence/analyze` remains deterministic (ACI-005). Failed live analysis does not store a `live_ai` row or invent metrics. Human decision authority is unchanged.

Live path proven against OpenAI `gpt-4o-mini` on localhost (`validate:aci009` PASS). Invalid mode and missing Goal fail closed. Missing-credentials detection unit-tested.

## Evidence (in repo)

- `ADE_ACI_009_LIVE_AI_ANALYTICS_COMPLETION_REPORT.md`
- `docs/nebula/artifacts/aci-009-evidence/`
- `scripts/validate-aci009.mjs`
- Instruction: `docs/nebula/aci/ACI-009.md`

## Gaps

- Only the OpenAI provider is implemented (`ADE_AI_PROVIDER=openai`). Other provider ids fail closed.
- Metrics remain operator-entered (`capture_method=manual`). Platform collection is still refused (409).
- Recommendations are advisory interpretation, not predictive guarantees.
