import { PlaceholderPanel } from "@/components/PlaceholderPanel";

export default function ReviewPage() {
  return (
    <PlaceholderPanel
      title="Review"
      laterUse="Human control point to inspect, edit, approve, or reject generated material before it can move toward publishing."
    />
  );
}
