import { PlaceholderPanel } from "@/components/PlaceholderPanel";

export default function AnalyticsPage() {
  return (
    <PlaceholderPanel
      title="Analytics"
      laterUse="Content and campaign performance capture. No metrics are collected in ACI-002."
    />
  );
}
