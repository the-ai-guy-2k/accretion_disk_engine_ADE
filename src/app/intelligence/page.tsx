import { PlaceholderPanel } from "@/components/PlaceholderPanel";

export default function IntelligencePage() {
  return (
    <PlaceholderPanel
      title="Intelligence"
      laterUse="Explain what ADE learned and recommend the next action. Recommendation records can be stored later in the SQLite foundation."
    />
  );
}
